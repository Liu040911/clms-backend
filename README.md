# clms-backend

Spring Boot 3.4.0 project scaffold (JDK 21).

Quick start (requires Maven and JDK 21 installed):

PowerShell:

```
mvn clean package
mvn spring-boot:run
```

or run the generated jar:

```
java -jar target\clms-backend-0.0.1-SNAPSHOT.jar
```
